{
  "scripts": {
    "dokku": {
      "predeploy": "rsync -av ./storage/frontend/ ./frontend/static/frontend/ && python manage.py migrate --noinput && python manage.py collectstatic --noinput"
    }
  }
}