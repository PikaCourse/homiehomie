{
  "scripts": {
    "dokku": {
      "predeploy": "rsync -rv --chmod=ugo=rwX ./storage/frontend/ ./frontend/static/frontend/ && python manage.py migrate --noinput && python manage.py collectstatic --noinput"
    }
  }
}