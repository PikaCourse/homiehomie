{
  "scripts": {
    "dokku": {
      "predeploy": "rsync -rv ./storage/frontend/ ./frontend/static/frontend/ && python manage.py migrate --noinput && python manage.py collectstatic --noinput"
    }
  }
}