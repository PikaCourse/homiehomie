{
  "scripts": {
    "dokku": {
      "predeploy": "chmod -R a+rwx ./storage/frontend/ && cp -r ./storage/frontend/ ./frontend/static/frontend/ && python manage.py migrate --noinput && python manage.py collectstatic --noinput"
    }
  }
}
